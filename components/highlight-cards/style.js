import styled from '@emotion/styled';

const HighlightCardItems = styled.div`
   width: 100%;
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(10rem, 20rem));
   grid-auto-rows: 6.25rem;
   grid-gap: 2rem;
   margin: 1rem auto;
`

export default HighlightCardItems;